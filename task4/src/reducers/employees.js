const empReducer = (state, action) => {
    switch(action.type){
        case 'ADDEMPLOYEE':
            return state;
        default:
            return state; 
    }
};

export default empReducer;